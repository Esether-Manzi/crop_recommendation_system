# Developer Journal

## Day 1 – Project Foundation

### Completed
- Created the Django project and application structure.
- Set up a Python virtual environment.
- Installed project dependencies.
- Installed and configured MySQL Server on Ubuntu 24.04.
- Resolved a MySQL installation issue caused by a port conflict with XAMPP.
- Created the project database (`crop_recommendation_db`).
- Created a dedicated database user (`crop_admin`) with appropriate privileges.

### Lessons Learned
- The difference between MySQL client libraries and the MySQL server.
- How to debug Linux services using `systemctl`.
- How to inspect MySQL logs.
- Why dedicated database users are preferable to using the `root` account.